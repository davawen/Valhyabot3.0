import { SlashCommandBuilder } from "discord.js";
import { CommandManager } from "../command_manager";

export function register(manager: CommandManager) {
	// const insultes = [
	//
	// ];
	//
	// manager.add_command(
	// 	new SlashCommandBuilder()
	// 		.setName("pute")
	// 		.setDescription("insulte tout le monde"),
	// 	interaction => {
	//
	// 	}
	// );
}
